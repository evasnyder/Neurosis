using UnityEngine;
using System.Collections;

public class Run : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		if (Input.GetKeyDown (KeyCode.RightShift) && Input.GetKeyDown (KeyCode.UpArrow)) { 

		} 
	}
}
